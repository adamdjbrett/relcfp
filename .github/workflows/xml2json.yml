name: XML to JSON
run-name: ${{ github.actor }} XML2JSON
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Data Format Converter Action
        uses: fabasoad/data-format-converter-action@v0.2.3
        id: xml2json
        with:
          input: "feed.xml"
          from: "xml"
          to: "json"
      - name: Print result
        run: echo "${{ steps.xml2json.outputs.output }}"